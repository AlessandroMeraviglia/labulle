# Only allow image and PDF files to be served from uploads
<FilesMatch "\.(?!(jpg|jpeg|png|webp|gif|pdf)$)">
    Require all denied
</FilesMatch>

# Disable script execution in uploads directory
php_flag engine off
